// src/tests/setup.ts
import { afterAll, beforeAll } from "vitest";

// Set up DB or mock tokens, etc.
beforeAll(async () => {
  // Create test users, clean tables, etc.
});

afterAll(async () => {
  // Cleanup
});
